git config --global user.name "Ankit"
git config --global user.email "as523617@gmail.com"
git config --list
git config --global core.editor emacs|
git config --global core.editor vim  | for changing environment into Linux

git config user.name
git config user.email
git status
git init
git status
git add --a
git commit -m "enter the message here" |For addition of message or else it will open Linux command prompt.

|| There are the code for adding a file to git ||
touch.gitignore


